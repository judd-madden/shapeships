/**
 * useBuildDraftSync
 * -----------------
 * Phase 3 stub.
 *
 * This hook will eventually own any debounced BUILD_DRAFT_SET syncing.
 * In the current codebase snapshot there is no draft-sync effect to extract,
 * so this is a no-op placeholder to establish the boundary.
 */

export function useBuildDraftSync(_args: {
  enabled: boolean;
  gameId: string | null;
  draft: any;
  onSync: (draft: any) => void;
}): void {
  // no-op (intentionally)
}
